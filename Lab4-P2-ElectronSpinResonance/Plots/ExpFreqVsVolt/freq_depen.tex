\documentclass{standalone}

\input{../../../plotPream}

\begin{document}

\begin{tikzpicture}
  \begin{axis}[
    title=Amplitude Vs Frequency,
    ylabel=Amplitude (\si{\volt}),
    xlabel=Frequency (\si{\hertz}),
    width=15cm,
    ]
    \addplot+ [only marks,
    error bars/.cd,
    x dir=both,
    x explicit,
    y dir=both,
    y explicit
    ] table[
    x=AdjustedFreq,
    y=Amplitude,
    x error expr=\thisrow{FreqError},
    y error expr=\thisrow{VError}
    ] {freq_depen.tsv};
  \end{axis}
\end{tikzpicture}

\end{document}
