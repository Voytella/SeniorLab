\documentclass{standalone}

\usepackage{pgfplotstable}
\usepackage{siunitx}

\begin{document}

\begin{tikzpicture}
  \begin{axis}[
    title=Zinc,
    ylabel=Voltage (\si{\milli\volt}),
    xlabel=Magnetic Field (\si{\milli\tesla}),
    width=15cm
    ]
    \addplot+[only marks, error bars/.cd,x dir=both, x explicit, y dir=both, y explicit] 
    table[x=Bx(mT),y=Vy(mV),x error expr=\thisrow{xErr(mT)},y error expr=\thisrow{yErr(mV)}] 
    {zincHall.dat};
    \addplot table[y={create col/linear regression={y=Vy(mV)}}] {zincHall.dat};
  \end{axis}
\end{tikzpicture}

\end{document}
