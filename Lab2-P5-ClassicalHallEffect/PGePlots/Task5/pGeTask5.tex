\documentclass{standalone}

\input{../../../plotPream}

\begin{document}

\begin{tikzpicture}
  \begin{axis}[
    title=pGe Task 5,
    ylabel=Hall Voltage (\( U_H \)) (\si{\volt}),
    xlabel=Temperature ( \( T \) ) (\si{\celsius}),
    width=15cm,
    x tick label style={rotate=0},
    y tick label style={rotate=0}
    ]
    \addplot+[only marks, 
    error bars/.cd, 
    x dir=both,
    x explicit,
    y dir=both,
    y explicit
    ] table[
    x=X, 
    y=Y, 
    x error expr=\thisrow{Xerr}, 
    y error expr=\thisrow{Yerr}
    ] {hallVoltVsTemp.tsv};
  \end{axis}
\end{tikzpicture}

\end{document}
