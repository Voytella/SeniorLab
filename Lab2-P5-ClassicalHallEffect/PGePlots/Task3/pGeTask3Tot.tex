\documentclass{standalone}

\input{../../../plotPream}

\begin{document}

\begin{tikzpicture}
  \begin{axis}[
    title=pGe Task 3,
    ylabel=Inverse Voltage (\si{\per\volt}),
    xlabel=Inverse Temperature (\si{\per\kelvin}),
    width=15cm
    ]
    \addplot+[only marks, 
    error bars/.cd, 
    x dir=both,
    x explicit,
    y dir=both,
    y explicit
    ] table[
    x=invTemp, 
    y=invVolt, 
    x error expr=\thisrow{invTempErr}, 
    y error expr=\thisrow{invVoltErr}]
    {pGeTask3Tot.dat};
  \end{axis}
\end{tikzpicture}

\end{document}

