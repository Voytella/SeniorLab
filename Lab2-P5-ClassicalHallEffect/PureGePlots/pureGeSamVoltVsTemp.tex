\documentclass{standalone}

\input{../../plotPream}

\begin{document}

\begin{tikzpicture}
  \begin{axis}[
    title=Pure Ge Sample Voltage Vs. Temperature,
    ylabel=Sample Voltage (\si{\volt}),
    xlabel=Temperature (\si{\celsius}),
    width=15cm,
    x tick label style={rotate=0},
    y tick label style={rotate=0}
    ]
%    \addplot table[
%    mark=none,
%    x=X,
%    y={create col/linear regression={y=Y}}
%    ] {samVoltVsTemp.tsv};
%    \addlegendentry{%
%      \( \pgfmathprintnumber{\pgfplotstableregressiona} I
%      \pgfmathprintnumber[print sign]{\pgfplotstableregressionb},
%      \sigma_{\text{fit}} = \num{3.50e-4} \)}%
    \addplot+[only marks, 
    error bars/.cd, 
    x dir=both,
    x explicit,
    y dir=both,
    y explicit
    ] table[
    x=X, 
    y=Y, 
    x error expr=\thisrow{Xerr}, 
    y error expr=\thisrow{Yerr}
    ] {samVoltVsTemp.tsv};
  \end{axis}
\end{tikzpicture}

\end{document}
