\documentclass{standalone}

\input{../../../plotPream}

\begin{document}

\begin{tikzpicture}
  \begin{axis}[
    title=Experimental Differential Cross Section,
    ylabel=Differential Cross Section (\si{\square\meter}),
    xlabel=Angle (deg),
    width=15cm,
    ]
    \addplot+ [only marks,
    error bars/.cd,
    x dir=both,
    x explicit,
    y dir=both,
    y explicit
    ] table[
    x=X,
    y=Y,
    x error expr=\thisrow{xErr},
    y error expr=\thisrow{yErr}
    ] {expCrossSec.tsv};
  \end{axis}
\end{tikzpicture}

\end{document}
